/**
 * Enum representing the different profiles a user can have
 */
export enum Profil {
  /**
   * Administrator profile
   */
  ADMIN = 'ADMIN',
  /**
   * Accountant profile
   */
  COMPTABLE = 'COMPTABLE',
}
